specVersion: "v1beta"
serviceId: "shopping"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "shopping-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
